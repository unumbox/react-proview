import React from 'react';
import ReactDOM from 'react-dom';
import ProviewClient from './ProviewClient';

ReactDOM.render(
  <React.StrictMode>
    <></>
  </React.StrictMode>,
  document.getElementById('root')
);
